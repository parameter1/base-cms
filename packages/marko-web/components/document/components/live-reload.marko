import isDev from '../../../utils/is-dev';

$ const { LIVERELOAD_PORT, LIVERELOAD_HOST } = process.env;

<if(isDev && LIVERELOAD_PORT)>
  <script src=`http://${LIVERELOAD_HOST}:${LIVERELOAD_PORT}/livereload.js` async></script>
</if>
