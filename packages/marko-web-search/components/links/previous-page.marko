$ const { prevPage } = input.$search;

<if(prevPage)>
  <marko-search-link
    class=input.class
    target=input.target
    rel=input.rel
    attrs=input.attrs
  >
    <@update param="page" with=prevPage />
    <${input.renderBody} />
  </marko-search-link>
</if>
