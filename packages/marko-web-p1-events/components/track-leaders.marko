import defaultValue from "@parameter1/base-cms-marko-core/utils/default-value";

$ const { site } = out.global;
$ const config = site.getAsObject("p1events");

$ const baseTenantKey = defaultValue(input.baseTenantKey, process.env.TENANT_KEY);
$ const props = { baseTenantKey, eventName: input.eventName };

<if(config.enabled)>
  <marko-web-browser-component name="P1EventsTrackLeaders" props=props />
</if>
