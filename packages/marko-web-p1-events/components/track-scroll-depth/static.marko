import ns from "../../utils/create-namespace";

$ const { site } = out.global;
$ const config = site.getAsObject("p1events");
$ const {
  entity,
  action,
  category = 'Static',
  selector,
  targetScrollDepths,
} = input;

<if(config.enabled && entity)>
  <marko-web-browser-component
    name="P1EventsTrackScrollDepth"
    props={
      entity,
      action,
      category,
      selector,
      targetScrollDepths,
    }
  />
</if>
