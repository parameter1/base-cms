import queryTotalCount from "../utils/query-total-count";

$ const { apollo } = out.global;
<marko-web-resolve|{ resolved: totalCount }|
  promise=queryTotalCount(apollo, { name: input.name, params: input.params })
>
  <${input.renderBody} total-count=totalCount />
</marko-web-resolve>
